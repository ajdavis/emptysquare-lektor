[model]
name = All Posts
hidden = yes

[fields.type]
name = Post Type
type = string
default = post

[children]
replaced_with = site.query('/blog').filter(F._model == 'blog-post' and F.type == this.type and F.pub_date).order_by('-pub_date')

[pagination]
enabled = yes
per_page = 10
