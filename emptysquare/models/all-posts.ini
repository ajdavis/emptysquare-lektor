[model]
name = All Posts
hidden = yes

[children]
replaced_with = site.query('/blog').filter(F._model == 'blog-post' and F.type == 'post').order_by('-pub_date')

[pagination]
enabled = yes
per_page = 10
