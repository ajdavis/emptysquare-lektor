[model]
name = Blog
label = {{ this.title }}
hidden = yes

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = markdown

[children]
order_by = -pub_date, title

[pagination]
enabled = yes
items = this.children.filter(F._model == 'blog-post')
per_page = 10
