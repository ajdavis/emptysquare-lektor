[model]
name = Category
label = {{ this.name }}
hidden = yes

[children]
replaced_with = site.query('/blog').filter(F.categories.contains(this.name))

[fields.name]
label = Name
type = string

[pagination]
enabled = yes
per_page = 10
