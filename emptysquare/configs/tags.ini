parent = /blog
template = tag.html
ignore_missing = true
items = site.query('/blog').filter(F.tags.contains(tag) and F._model == 'blog-post' and F.type == 'post' and F.pub_date).order_by('-pub_date')
